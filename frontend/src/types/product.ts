export interface Product {
  id: string;        // each product has an id
  name: string;      // a name
  price: number;     // price in Naira
  image: string;     // URL to product image
  description: string; // description text
}
